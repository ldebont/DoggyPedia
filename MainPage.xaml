<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:DoggyPedia.Model"
             xmlns:data="clr-namespace:DoggyPedia.Data"
             x:Class="DoggyPedia.MainPage"
             Title="Dog Breeds">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <SearchBar Grid.Row="0" 
                   Placeholder="Find a dog breed..."
                   FontSize="Medium"
                   TextChanged="OnSearchQueryChanged"/>

        <CollectionView Grid.Row="1"
                        x:Name="DogBreeds"                        
                        ItemsSource="{x:Static data:DogData.Dogs}"
                        ItemsLayout="VerticalList"
                        SelectionMode="Single"
                        SelectionChanged="OnDogSelectionChangedAsync">

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Dog">
                    <HorizontalStackLayout Padding="10"
                                           Spacing="15">
                        <Image
                       Source="{Binding ImagePath}"
                       Aspect="AspectFill"
                       HeightRequest="80"
                       WidthRequest="80"/>
                        <Label
                       Text="{Binding Name}"
                       FontAttributes="Bold"
                       FontSize="Medium"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"/>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup>
                                <VisualState Name="Normal"/>
                                <VisualState Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Transparent"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>